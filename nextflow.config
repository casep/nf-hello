params {

    help = false
    version = false

    disable_ping = false
    threads = 4

    aws_image_prefix = null
    aws_queue = null

    out_dir = "output"

    // feature flags
    snp = false
}
process {
    container  = 'quay.io/nextflow/bash'
}